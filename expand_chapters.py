# -*- coding: utf-8 -*-
"""
扩展章节定义：新增 107 章（编号 94-200）。
四大板块：
  A. Transformer 原理深挖（94-119，26 章）
  B. 向量索引与 RAG 原理深挖（120-145，26 章）
  C. 开源 Agent 源码深挖（146-178，33 章）
  D. 进阶综合补全（179-200，22 章）

每个条目： (slug, title, group, blocks)
blocks 是内容块列表，每块 (type, content)：
  ('h2', '标题')        → <h2>
  ('p', '文字')          → <p>
  ('formula', 'LaTeX')   → 公式块
  ('code', '代码')       → <pre><code>
  ('table', [[行],...])  → 表格
  ('callout', '文字')    → 提示框
  ('h3', '标题')         → <h3>
"""

# ============================================================
# A. Transformer 原理深挖（26 章）
# ============================================================
TRANSFORMER_DEEP = [
    ('tfm-attention-math', '注意力的数学本质：从点积到 Softmax', [
        ('h2', '01 一行公式背后的全部秘密'),
        ('p', 'Transformer 的灵魂是一个公式：Attention(Q, K, V) = softmax(QK^T / √d_k) V。这一行写起来轻巧，却浓缩了"相关性度量 + 归一化 + 加权聚合"三件事。本章我们把这一行拆成零件，逐步推导，让你看清每个符号在做什么、为什么除以 √d_k、softmax 为什么不可省。'),
        ('h3', '1.1 为什么用点积衡量相关性'),
        ('p', '点积是衡量两个向量"方向是否一致"的最经济手段。当 Q（query，我在找什么）与 K（key，每个候选被打上的标签）越对齐，点积越大，意味着"这个候选越符合我的需求"。注意点积没有归一化，其数值随维度 d_k 线性放大——这埋下了"大数值压垮 softmax"的隐患。'),
        ('formula', r'\text{score}(q, k) = q \cdot k = \sum_{i=1}^{d_k} q_i \, k_i'),
        ('p', '用一个 toy 例感受：d_k = 4 时，两个标准正态向量的点积期望是 0、方差是 4（标准差 2）；当 d_k = 512 时，方差涨到 512（标准差 ~22.6）。原始分数动辄几十几百。'),
        ('h3', '1.2 缩放因子 √d_k 到底救了谁'),
        ('p', 'softmax 对输入大小极其敏感：当输入的最大值远超其它，softmax 输出会趋近 one-hot，梯度几乎归零（所谓饱和区）。如果不除以 √d_k，高维下 QK^T 的元素普遍很大，softmax 会变得"过于自信"——只盯着一个位置，反向传播时梯度消失，训练停滞。'),
        ('formula', r'\text{Attention}(Q,K,V) = \mathrm{softmax}\!\left(\frac{QK^{\top}}{\sqrt{d_k}}\right) V'),
        ('p', '除以 √d_k 把分数的标准差拉回 ~1 的量级，让 softmax 保持在梯度良好的"温和自信"区间。这是一个看似随意实则精妙的工程选择：它让模型在任何维度下都能稳定训练。'),
        ('callout', '记忆口诀：点积量相关性 → 除根号防爆炸 → softmax 归一成权重 → 用权重混合 V 得到输出。'),
        ('h2', '02 手算一遍：3×3 注意力矩阵'),
        ('p', '纸上推一遍比看十遍代码都有用。设 d_k = 2，3 个 token 的 Q、K、V 如下（随意取的小整数，方便心算）：'),
        ('formula', r'Q = K = \begin{bmatrix} 1 & 0 \\ 0 & 1 \\ 1 & 1 \end{bmatrix},\quad V = \begin{bmatrix} 10 \\ 20 \\ 30 \end{bmatrix}'),
        ('p', '第一步，算 QK^T（点积矩阵，第 i 行第 j 列是第 i 个 query 对第 j 个 key 的原始分数）：'),
        ('formula', r'QK^{\top} = \begin{bmatrix} 1 & 0 & 1 \\ 0 & 1 & 1 \\ 1 & 1 & 2 \end{bmatrix}'),
        ('p', '第二步，除以 √d_k = √2 ≈ 1.414，得缩放分数：[[0.71,0,0.71],[0,0.71,0.71],[0.71,0.71,1.41]]。第三步，对每一行做 softmax。以第一行为例：e^0.71≈2.03, e^0=1, e^0.71≈2.03，和≈5.06，归一后 ≈ [0.40, 0.20, 0.40]。'),
        ('p', '第四步，用权重乘 V：0.40×10 + 0.20×20 + 0.40×30 = 4+4+12 = 20。于是第一个 token 的输出是 20。你会发现：token 1（[1,0]）和 token 1、token 3 最相关，所以输出主要由 V_1 和 V_3 决定，V_2 只占 20%——这正是"注意到了相关的位置"。'),
        ('callout', '亲手算一遍你就理解了：注意力不是黑盒，它就是"按相关性加权求和"，而相关性用缩放点积度量。'),
    ]),
    ('tfm-qkv-projection', 'Q/K/V 投影：为什么要三个不同的矩阵', [
        ('h2', '01 同一组向量，为什么不能直接当 Q、K、V'),
        ('p', '初学者的第一个疑问：既然输入就是一串 token 向量，为什么不直接拿它们做 Q、K、V，非要再乘三个矩阵 W_Q、W_K、W_V？答案是"角色分工"。同一个词，在"作为被查询的内容"和"作为查询者"和"作为被提取的信息"时，需要强调的特征完全不同。投影矩阵让模型学会从同一个输入里抽出三种不同视角的表示。'),
        ('p', '举例："苹果"这个词向量在作为 K（被检索）时，可能需要突出"它是一种水果"这个语义标签，让别人能检索到它；而作为 V（被聚合的信息）时，可能需要携带它的颜色、口感等细节；作为 Q（查询者）时，它可能在找"和苹果相关的其他词"。三种角色，三个投影方向。'),
        ('formula', r'Q = X W_Q,\quad K = X W_K,\quad V = X W_V'),
        ('p', 'X 是输入（n×d），三个 W 都是可学习参数矩阵（d×d_k 或 d×d_v）。这三个投影是注意力机制里可训练参数的主要来源——它们承担了"如何从原始表示里提取查询、标签、内容三种信号"的全部学习任务。'),
        ('h2', '02 投影矩阵的维度选择'),
        ('p', '实践中 d_k（Q、K 的维度）和 d_v（V 的维度）可以不同。原始论文里 d_k = d_v = d_model / h，其中 h 是头数。为什么 Q、K 同维？因为它们要做点积——只有维度相同才能点积。V 的维度则决定输出的信息容量，通常也设成与 d_k 相同以简化工程。'),
        ('h3', '2.1 输出投影 W_O'),
        ('p', '多头注意力最后还有一个 W_O 矩阵，把拼接后的多头输出投影回 d_model 维。它负责"融合多个头各自学到的不同关注模式"。没有 W_O，多个头的输出只是简单堆叠，无法交互；有了 W_O，模型可以学习"头 1 看语法、头 2 看语义，最后按比例合成"这样的组合。'),
        ('formula', r'\text{MultiHead}(Q,K,V) = \mathrm{Concat}(\text{head}_1,\dots,\text{head}_h)\, W_O'),
        ('callout', '一句话：W_Q/W_K/W_V 负责把输入"切"成三个角色，W_O 负责把多头结果"合"回统一表示。注意力层的所有可学参数都在这四个矩阵里。'),
    ]),
    ('tfm-positional-encoding', '位置编码：没有它 Transformer 就是个瞎子', [
        ('h2', '01 为什么必须告诉模型"顺序"'),
        ('p', '注意力的计算对输入顺序完全无感——打乱 token 的顺序，只要 Q、K、V 重新对应打乱后的位置，输出只跟着重排，数值关系不变。这叫置换不变性（permutation invariance）。但语言显然有顺序："狗咬人"和"人咬狗"天差地别。所以必须额外注入位置信息，这就是位置编码（Positional Encoding, PE）。'),
        ('h2', '02 正弦余弦编码：原始 Transformer 的方案'),
        ('p', '论文 "Attention is All You Need" 用固定的正弦/余弦函数生成位置编码，每个维度用不同频率的三角波：'),
        ('formula', r'PE_{(pos,2i)} = \sin\!\left(\frac{pos}{10000^{2i/d}}\right),\quad PE_{(pos,2i+1)} = \cos\!\left(\frac{pos}{10000^{2i/d}}\right)'),
        ('p', '为什么用三角函数？因为它有一个绝妙性质：对于任意固定的偏移 k，PE(pos+k) 可以表示成 PE(pos) 的线性函数。这意味着模型理论上能学会"相对位置"——通过一个线性变换从当前位置的编码推出 k 步之后的编码。高频维度编码精细的局部位置，低频维度编码粗略的全局位置，形成多尺度的位置感知。'),
        ('h3', '2.1 和词嵌入相加而非拼接'),
        ('p', 'PE 与词嵌入是逐元素相加（element-wise add），不是拼接。相加的好处是不增加维度，且模型后续的线性层可以"解调"出位置和内容两个信号（因为它们在不同频率的子空间里，可被线性分离）。这看似粗暴实则有效——实践证明相加完全够用。'),
        ('h2', '03 可学习位置编码与外推难题'),
        ('p', 'BERT、GPT 普遍改用可学习的位置编码：给每个位置一个可训练的向量，存在一张查找表里。优点是灵活、效果好；缺点是无法外推——训练时只见过长度的位置，推理时序列更长就没法编码。这就是为什么早期 GPT 的上下文窗口是硬上限。'),
        ('callout', '外推难题是 RoPE 要解决的核心问题。可学习 PE 在训练长度内无敌，超出就废；正弦 PE 能外推但效果稍逊。RoPE 用旋转把相对位置编码进注意力，兼顾两者。'),
    ]),
    ('tfm-rope', 'RoPE 旋转位置编码：现代大模型的选择', [
        ('h2', '01 把绝对位置藏进相对关系'),
        ('p', '旋转位置编码（Rotary Position Embedding, RoPE）是苏剑林等在 2021 年提出的方案，如今是 LLaMA、GLM、Qwen 等几乎所有主流大模型的标配。它的核心思想：不直接给位置加偏置，而是通过旋转向量，使得任意两个位置的注意力分数只依赖它们的相对距离。'),
        ('h2', '02 旋转的几何直觉'),
        ('p', '把 d 维向量看成 d/2 个二维平面，在每个平面上做不同角度的旋转。位置 m 处的向量旋转 m·θ_i 角度（θ_i 是该平面的基础频率）。当位置 m 的 query 和位置 n 的 key 做点积时，旋转角度差是 (m-n)·θ_i——于是点积只依赖相对距离 m-n。'),
        ('formula', r'q_m^{\top} k_n = \mathrm{Re}\!\left( (q \cdot e^{im\theta})^* (k \cdot e^{in\theta}) \right)\ \text{依赖}(m-n)'),
        ('p', '用旋转矩阵实现：q\' = R_m q，k\' = R_n k，其中 R_m 是位置 m 对应的块对角旋转矩阵。点积 q\'^T k\' = q^T R_m^T R_n q = q^T R_{n-m} k，只含相对角度。这就是 RoPE 的全部数学。'),
        ('h3', '2.1 旋转矩阵的分块形式'),
        ('formula', r'R_m = \mathrm{diag}\!\left(\begin{bmatrix}\cos m\theta_1 & -\sin m\theta_1 \\ \sin m\theta_1 & \cos m\theta_1\end{bmatrix}, \dots, \begin{bmatrix}\cos m\theta_{d/2} & -\sin m\theta_{d/2} \\ \sin m\theta_{d/2} & \cos m\theta_{d/2}\end{bmatrix}\right)'),
        ('p', 'θ_i = 10000^(-2i/d)。高频平面（小 i）旋转快，编码近处差异；低频平面（大 i）旋转慢，编码远处差异。和正弦编码的频率设计一脉相承。'),
        ('h2', '03 为什么 RoPE 能外推 + 长上下文技巧'),
        ('p', '因为相对距离是连续的函数，理论上任意大的 m-n 都有定义——这就是外推能力的来源。但实践中直接外推到训练长度 4 倍以上，效果仍会掉（因为低频维度的旋转角度分布外推后分辨率不足）。解决方案有 Position Interpolation（PI，把位置缩放到训练范围内）和 NTK-aware 插值（调整 θ 的底数），YaRN、LongRoPE 等都是这条线的改进。'),
        ('callout', '现代长上下文（128K、1M）几乎都用 RoPE + 插值方案。RoPE 本身解决"相对位置"，插值解决"超出训练长度"。'),
    ]),
]
